#!/usr/bin/env bash
#MISE description="Run all Go tests with the race detector"
set -euo pipefail

source scripts/lib/env.sh
load_project_env
: "${TEST_DATABASE_URL:?TEST_DATABASE_URL is required}"
cd backend
go test -race ./...
